# Scenarios — 黄金路径与成功标准

## 格式

每条场景包含：

- **场景名**：简短标识
- **输入**：患者主诉文本
- **预期管线**：实体抽取 → M-rule 匹配 → 图谱检索 → 向量检索 → SOAP 输出
- **成功标准**：SOAP 报告应包含哪些关键字段
- **验收状态**：✅ 通过 / ❌ 失败 / ⏳ 待测试

---

> *本文件将在 orchestrator 实现后，用真实病例运行验证时填充。*